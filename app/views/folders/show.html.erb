<% @sub_folders.each do |folder| %>
    <%= link_to folder.name, folder_path(folder) %>
<% end %>



<li><%= link_to "New Sub-Folder", folder_new_sub_folder_path(@folder) %></li>

<li><%= link_to "Upload file", folder_new_sub_file_path(@folder) %></li>


<% @sub_files.each do |file| %>
    <p>Shooting date: <%= file.shooting_date %></p>
    <p>Deletion date: <%= file.delete_date %></p>
    <p>Tags: <%= raw file.tags.map(&:name).map { |t| link_to t, tag_path(t) }.join(', ') %></p>
        <div><% file.photos.each do |attachment| %>  
                <%= link_to attachment.filename, photo_path(file) %>
            <% end %>
        </div>
<% end %>
   

   link_to (image_tag image.variant(resize: "50%", quality: 75).processed, class: "card-img-top"), photo_path(image_all)