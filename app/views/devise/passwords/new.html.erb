<div class="container">
  <div class="row">

		<div class="col">
		</div>

		<div class="col"> 
			<h1>Forgot password?</h1>
		</div>

		<div class="col">
		</div>

		<div class="col">
		</div>

	</div>

    <div class="row">

        <div class="col">
        </div>
          
        <div class="col">

			<%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
			<%= render "devise/shared/error_messages", resource: resource %>

			<div class="field user-input">
				<%= f.label :email %><br />
				<%= f.email_field :email, autofocus: true, autocomplete: "email" %>
			</div>

			<div class="actions">
				<%= f.submit "Send instructions", class: "btn btn-primary btn-space" %>
			</div>
			<% end %>

			<%= render "devise/shared/links" %>
        </div>

		<div class="col">
			<%= image_tag(asset_path('logo_black.png'), class: "mx-auto d-block") %>
		</div>

		<div class="col">
		</div>

    </div>
</div>


